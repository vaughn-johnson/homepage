import { Null } from 'components';
import { colors } from 'styles';

const errorMessage = {
  background: colors.orange,
  color: 'white',
  fontFamily: 'Poppins',
  fontWeight: 300,
  fontSize: '3em',
  textAlign: 'center',
  marginTop: '10%',
  marginBottom: '10%'
};

export {
  errorMessage 
};

export default Null;